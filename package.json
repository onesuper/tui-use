{
  "name": "termlink",
  "version": "0.1.0",
  "description": "Interactive CLI bridge for AI coding agents — drive any interactive terminal program programmatically",
  "bin": {
    "termlink": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@xterm/headless": "^6.0.0",
    "commander": "^12.0.0",
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
